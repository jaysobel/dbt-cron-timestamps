[project]
name = "dbt-cron-timestamps"
version = "1.1.0"
description = "Development tooling for the dbt Cron to Timestamps package"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
  "croniter==6.2.4",
  "cronsim==2.7",
  "dbt-snowflake>=1.10,<1.11",
]

[tool.uv]
package = false
