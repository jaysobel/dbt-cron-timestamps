name: 'dbt_cron_timestamps'
version: '1.1.0'
config-version: 2
require-dbt-version: [">=1.5.0", "<3.0.0"]

profile: 'dbt_cron_timestamps'

macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"
